#include <stdio.h>
#include <stdlib.h>
/**
 * The start of the main function
 * return is always 0
 */
int main(void)
{
	char a;
	for (a='a';a<='z'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return 0;
}
